# Python 3.10+ recommended. Install torch for your platform first if needed:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
numpy>=1.24
opencv-python>=4.8
matplotlib>=3.7
pillow>=10.0
torch>=2.0
# Segment Anything 2 (Meta) — install from GitHub
sam-2 @ git+https://github.com/facebookresearch/sam2.git
